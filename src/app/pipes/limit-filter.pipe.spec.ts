import { LimitFilterPipe } from './limit-filter.pipe';

describe('LimitFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
